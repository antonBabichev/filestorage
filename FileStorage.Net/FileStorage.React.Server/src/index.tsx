import React from 'react';
import ReactDOM from "react-dom/client";
import { MainContainer } from '@filestorage/client';

ReactDOM.createRoot(document.getElementById("root")).render(<MainContainer />);
